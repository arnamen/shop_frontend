import React from 'react'

import Form from '../UI/Form/Form';

export default function RegisterModal( props ) {
    return (
        <Form>
            <Form.Label for='field'>TEST</Form.Label>
            <Form.TextField id='field'/>
        </Form>
    )
}
